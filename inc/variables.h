#ifndef VARIABLES_H
#define VARIABLES_H

struct blockPos // this variable is the x and y of an object
{
    int x;
    int y;
};

struct blockSize // this variable is the width and height of an object
{
    int width;
    int height;
};

#endif
